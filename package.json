{
  "name": "client",
  "version": "0.0.0",
  "scripts": {
    "build": "preact build --no-prerender --no-service-worker --template src/template.html && rm -r build/assets/icon.png build/manifest.json build/push-manifest.json build/assets build/polyfills* && cp src/assets/* build",
    "dev": "preact watch --template src/template.html",
    "format": "prettier --write **/*",
    "serve": "preact build && preact serve",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev"
  },
  "dependencies": {
    "@zeiw/trump": "^1.0.5",
    "clipboard": "^2.0.4",
    "howler": "^2.1.2",
    "jss": "^9.8.7",
    "jss-camel-case": "^6.1.0",
    "jss-nested": "^6.0.1",
    "jss-vendor-prefixer": "^8.0.1",
    "micromodal": "^0.4.0",
    "preact": "^8.2.1",
    "socket.io-client": "^2.2.0",
    "tachyons": "^4.11.1",
    "tippy.js": "^4.3.5"
  },
  "devDependencies": {
    "if-env": "^1.0.0",
    "preact-cli": "^2.0.0",
    "prettier": "^1.18.2"
  }
}
